# Copy to .env and adjust as needed. All values are optional.

HOST=0.0.0.0
PORT=8000

MAX_SEARCH_RESULTS=5
MAX_ORGANIZE_FILES=50

# Optional AI fallback for unknown commands (roadmap)
USE_AI_FALLBACK=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

DEBUG=true
